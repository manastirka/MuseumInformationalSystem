# Progress - 2026-03-18

## Scope

Continued the `app.py` modularization pass by extracting the remaining inline curator collection bootstrap surface: fallback payloads, lazy collection loaders, and heritage/meteorite database access helpers.

## Work Completed Today

- Added `collection_bootstrap_support.py` as the new home for curator collection bootstrap data and loader logic.
- Moved the seeded fallback payloads for:
  - botany
  - ichthyology
  - entomology
  - mycology
  - herpetology
  - ornithology
  - paleozoology
  - paleobotany
  - petrology
- Moved the seeded bootstrap databases for:
  - meteorite collection
  - conservation biology
  - cultural heritage
- Added `CollectionBootstrapSupport`, which now owns:
  - PostgreSQL-or-fallback curator collection loading
  - lazy collection database construction via `LazyLoadedDict`
  - cached Phase 3A heritage and meteorite getters
- Replaced the corresponding large inline `app.py` blocks with thin alias wiring so existing names stay stable:
  - `load_collection_database`
  - `BOTANY_COLLECTION_DATABASE`
  - `ICHTHYOLOGY_COLLECTION_DATABASE`
  - `ENTOMOLOGY_COLLECTION_DATABASE`
  - `MYCOLOGY_COLLECTION_DATABASE`
  - `HERPETOLOGY_COLLECTION_DATABASE`
  - `ORNITHOLOGY_COLLECTION_DATABASE`
  - `PALEOZOOLOGY_COLLECTION_DATABASE`
  - `PALEOBOTANY_COLLECTION_DATABASE`
  - `PETROLOGY_COLLECTION_DATABASE`
  - `METEORITE_COLLECTION_DATABASE`
  - `CONSERVATION_BIOLOGY_DATABASE`
  - `CULTURAL_HERITAGE_DATABASE`
  - `get_cultural_heritage_database`
  - `get_meteorite_collection_database`
- Preserved the public surface expected by the already-extracted modules (`collection_management_views.py`, `collection_access_support.py`, `museum_overview_views.py`, `collection_media_views.py`) so no wrapper signatures or call sites had to change.
- Added `vehicle_data_support.py` and moved the vehicle/reservation persistence logic there:
  - vehicle JSON/PostgreSQL loading
  - vehicle JSON saving
  - reservation JSON/PostgreSQL loading
  - reservation JSON saving
- Kept the public cache globals and app-level names stable in `app.py` for:
  - `load_vehicles`
  - `save_vehicles`
  - `load_reservations`
  - `save_reservations`
  - `get_museum_vehicles`
  - `get_vehicle_reservations`
- Added `museum_staff_support.py` and moved the library/employee-directory bootstrap logic there:
  - library JSON/PostgreSQL loading
  - library JSON saving with statistics refresh
  - employee directory JSON/PostgreSQL loading
- Kept the app-level lazy globals and names stable in `app.py` for:
  - `load_library_database`
  - `save_library_database`
  - `get_library_database`
  - `load_employee_directory`
  - `get_employee_directory`
- Added `module_access_support.py` and moved the persistence logic there for:
  - module access JSON load/merge behavior
  - module access JSON saving
  - dashboard preferences JSON loading
  - dashboard preferences JSON saving
- Kept the app-level mutable globals, mtime tracking, and public names stable in `app.py` for:
  - `_get_file_mtime`
  - `load_module_access`
  - `save_module_access`
  - `load_dashboard_preferences`
  - `save_dashboard_preferences`
- Added `fallback_auth_support.py` and moved the fallback auth logic there for:
  - fallback employee payload creation
  - fallback admin authentication
- Kept the app-level cache and one-time state stable in `app.py` for:
  - `MUSEUM_EMPLOYEES`
  - `get_museum_employees`
  - `get_fallback_employees`
  - `authenticate_fallback_user`
  - `_login_tracker_initialized`
  - `_fallback_auth_warning_logged`
  - `ensure_login_tracker_initialized`
  - `log_fallback_auth_warning_once`
- Added `app_ui_support.py` and moved the remaining dashboard/collection display helper logic there for:
  - module list selection for dashboard rendering
  - QR-highlight collection-page filtering bridge
- Kept the app-level compatibility names stable in `app.py` for:
  - `get_user_modules`
  - `prepare_collection_records_for_display`
- Added `app_core_support.py` and moved the remaining generic app-core utilities there:
  - `LazyServiceProxy`
  - `LazyLoadedDict`
  - `current_user_is_admin`
  - `can_access_owned_record`
- Kept the app-level names stable in `app.py` as aliases for those app-core utilities.
- Reduced `app.py` to `3720` lines after the full set of today’s extractions.

## Validation

- `python3 -m py_compile app.py collection_bootstrap_support.py`
- `python3 -m unittest test_startup_lazy_loading.py test_timesheet_route_refactor.py`
- `python3 -m unittest test_production_readiness.py test_authorization_regressions.py`
- Final combined regression count after this slice: `Ran 264 tests`
- Result: `OK`

## Current State

- The largest remaining inline curator collection/bootstrap payloads are no longer embedded directly in `app.py`.
- `app.py` now keeps alias glue for this area instead of owning the fallback datasets and cached heritage/meteorite access logic inline.
- The extracted collection/admin route modules continue to work against the same names they used before this change.
- Vehicle fallback/cache persistence no longer lives inline in `app.py`; only the cache state and compatibility wrappers remain there.
- Library and employee-directory bootstrap logic no longer lives inline in `app.py`; only the lazy globals and compatibility wrappers remain there.
- Module access and dashboard preference file persistence no longer lives inline in `app.py`; only the mutable state, mtime tracking, and compatibility wrappers remain there.
- Fallback employee/authentication behavior no longer lives inline in `app.py`; only the app-local cache and one-time warning/tracker state remain there.
- Dashboard module selection and collection highlight bridge helpers no longer live inline in `app.py`; compatibility wrappers remain there.
- Generic lazy-loading and authorization utility primitives no longer live inline in `app.py`; they now live in `app_core_support.py` and are re-exported from the app module for compatibility.
- Startup and wrapper regression coverage remain green after the extraction.

## Additional Work Completed Later Today

- Performed a careful root-folder cleanup and archived obvious relics/clutter instead of hard-deleting them:
  - created `backups/root_cleanup_20260318/`
  - moved old `app.py` backups, generated reports, generated test outputs, stray temp files, and likely relic scripts/service variants there
  - moved loose progress/setup/context notes into `docs_archive/root_notes_20260318/`
  - updated `docs_archive/2026-03-18_ROOT_FOLDER_CLEANUP.md`
- Reorganized low-risk standalone utility scripts from the repo root into categorized `scripts/` folders without disturbing app/runtime files.
- Archived likely relic files after dependency/reference review:
  - `museum-system.service.new`
  - `museum-system.service.simple`
  - `museum-system.service.updated`
  - `timesheet_full_system.py`
  - `word_export_ultra_fast.py`
  - `database_migrations.py`
  - `setup_employees.py`
- Admin panel cleanup in `templates/admin_panel.html`:
  - fixed per-card minimize behavior so it works correctly with multiple cards/items on the page
  - removed maximize controls
  - removed search-panel min/max-all controls
  - removed the statistics section/cards from the admin page
- Mail client phishing-hardening pass:
  - updated `mail_client.py`
  - updated `templates/admin_mail_client.html`
  - added server-side HTML sanitization for message previews
  - disabled direct clickable links inside HTML previews
  - tightened iframe sandboxing for email HTML
  - added security metadata for:
    - external sender
    - `Reply-To` mismatch
    - suspicious links
    - dangerous attachment types
  - added warning UI badges/panel in the reader
  - added safe link-open confirmation flow
  - added dangerous attachment confirmation flow
  - added local cache backfill/migration behavior for new message security fields
- Mail client folder column logic improvement:
  - reordered the folder list client-side so `Пристигле` stays first
  - then sent, drafts, archive, spam/junk, trash
  - custom folders fall back to alphabetical order after the standard ones
- Dashboard/admin first-load fix:
  - fixed stale first-render behavior where admin sometimes saw only one dashboard item until a hard reload
  - root cause: `app_ui_support.get_user_modules()` loaded fresh JSON-backed state but still filtered with stale dict snapshots passed from `app.py`
  - fix: use the freshly loaded return values from `load_module_access()` and `load_dashboard_preferences()`
  - also added no-cache headers in `core_app_views.render_dashboard()` to reduce stale authenticated dashboard responses in the browser
  - added regression coverage in `test_startup_lazy_loading.py`

## Additional Validation Later Today

- `python3 -m unittest test_timesheet_route_refactor.py`
  - repeated several times after admin panel, dashboard, mail client, and folder-order UI changes
  - result stayed `Ran 230 tests`, `OK`
- `python3 -m unittest test_startup_lazy_loading.py`
  - after the dashboard first-load fix
  - result: `Ran 21 tests`, `OK`

## PostgreSQL Environment Note

- Important nuance for next session:
  - the app is configured from `.env` with `DATABASE_URL=postgresql+psycopg://aleksandarlukovic@localhost:5432/museum_system`
  - in Python, `app.py` therefore imports `mineral_database_pg` and configures curator collection loading through PostgreSQL-aware paths
- However, the Codex workspace session could not reach `localhost:5432`:
  - `pg_isready -h localhost -p 5432` inside the workspace returned `no response`
  - direct `psycopg` connection attempts from the workspace failed
  - this is an environment-isolation issue, not evidence that the host PostgreSQL service is down
- User-provided host output later confirmed:
  - `postgresql.service` on the host machine was `active (running)` since `2026-03-18 09:00:35 CET`
- Practical implication:
  - PostgreSQL-related warnings seen in Codex-run tests/logs can be artifacts of the isolated workspace not seeing the host loopback service
  - do not assume the real app instance is down just because the workspace session cannot connect to `localhost:5432`

## Next Target

The major `app.py` modularization pass is effectively complete. The next session should focus on higher-value follow-up work rather than another broad extraction sweep.

Practical next candidates:

- continue the mail-client phishing hardening with higher-signal checks such as:
  - SPF/DKIM/DMARC header parsing/display
  - stronger per-link domain mismatch banners
  - optional server-side phishing score refinement
- if needed, do a final review pass on remaining app-core glue in `app.py`, but there is no obvious large modularization slice left
- if running from a higher-access instance, verify PostgreSQL connectivity from that environment before trusting any Codex-side Postgres failure logs

## Additional PostgreSQL Migration and Validation Later Today

- Verified from a higher-access instance that the host PostgreSQL service was reachable:
  - `pg_isready -h localhost -p 5432` returned `accepting connections`
  - direct `psycopg` connection to `museum_system` succeeded
  - server version reported `PostgreSQL 16.11`
- Audited live database usage and confirmed:
  - main runtime is PostgreSQL-first
  - SQLite was still active in the live paths for:
    - `bird_ringing_database.py`
    - `mail_client.py`
    - `chat_room.py`
    - `scientific_papers_database.py`
    - `inventory_reconciliation.py`
    - `map_feature_paper_enricher.py`
  - MySQL references were only legacy/config/dependency leftovers, not the active runtime path
- Migrated the remaining active runtime SQLite-backed features to PostgreSQL:
  - updated `chat_room.py`
  - updated `mail_client.py`
  - updated `scientific_papers_database.py`
  - updated `map_feature_paper_enricher.py`
  - updated `inventory_reconciliation.py`
  - updated `bird_ringing_database.py`
  - updated `postgres_service.py` for direct module `.env` loading
  - added schema definitions to `db/schema.sql`
  - added migration script `migration/007_remaining_sqlite_runtime_to_postgres.sql`
  - added data migration script `scripts/migrate_remaining_runtime_sqlite_to_postgres.py`
- Ran the runtime data migration into PostgreSQL and verified populated tables:
  - `scientific_papers`: `1214`
  - `paper_locality_links`: `1586`
  - `paper_feature_links`: `1203`
  - `chat_messages`: `5`
  - `chat_presence`: `1`
  - `chat_unread_cursors`: `1`
  - `mail_cache_folders`: `18`
  - `mail_cache_messages`: `451`
  - `mail_cache_meta`: `2`
  - `mail_cache_pending_reads`: `0`
- Removed the remaining dead SQLite implementation from `bird_ringing_database.py`:
  - the module is now PostgreSQL-only
  - preserved the public API and a compatibility `get_connection()` entry point
- Fixed a PostgreSQL row-shape regression in `mail_client.py`:
  - SQLite-style tuple indexing was still present in several cache/count/meta reads
  - replaced those accesses with named-row-safe handling and query aliases
- Converted the old standalone PostgreSQL verification scripts into discovered automated tests:
  - `test_postgres_connection.py`
  - `test_postgres_implementation.py`
  - added explicit `engine.dispose()` teardown to avoid `psycopg` `ResourceWarning` noise at interpreter shutdown

## Additional Validation and Testing Later Today

- `python3 -m py_compile chat_room.py scientific_papers_database.py map_feature_paper_enricher.py inventory_reconciliation.py mail_client.py bird_ringing_database.py scripts/migrate_remaining_runtime_sqlite_to_postgres.py postgres_service.py`
  - result: `OK`
- Runtime smoke tests passed against PostgreSQL for:
  - `mail_client`
  - `chat_room`
  - `scientific_papers_database`
  - `inventory_reconciliation`
  - `bird_ringing_database`
  - `map_feature_paper_enricher`
- Standalone PostgreSQL validation scripts were executed before conversion:
  - `python3 test_postgres_connection.py` -> success
  - `python3 test_postgres_implementation.py` -> `6/6` checks passed
- After conversion to discovered tests:
  - `python3 -m unittest test_postgres_connection.py test_postgres_implementation.py`
  - result: `Ran 8 tests`, `OK`
- Full top-level regression suite after the migration/testing cleanup:
  - `python3 -m unittest discover -s /home/aleksandarlukovic/MuseumInfoSystem -p 'test_*.py'`
  - result: `Ran 295 tests`, `OK`
- Remaining known warning after the full suite:
  - Werkzeug deprecation warning about cookie values being passed as bytes instead of strings
  - the earlier temporary `psycopg` `ResourceWarning` noise was resolved by the test teardown fix

## Additional UI/Nav Cleanup Later Today

- Removed the centered navbar ornament/background detail that was showing behind `Возила` and `Базе података`
  - updated `static/css/main.css`
- Normalized top-nav icon/text alignment so icons and labels stay on one line consistently
  - updated `static/css/main.css`
- Refined the top navigation for laptop-width admin use:
  - avoided the too-tall collapsed-menu approach
  - restored the standard desktop navbar breakpoint in `templates/base.html`
  - introduced an adaptive compacting strategy:
    - wide screens keep the full top-level items
    - narrower desktop widths use a grouped `Више` dropdown for lower-priority links
  - moved these lower-priority links into the adaptive compact dropdown when needed:
    - `Терминологија`
    - `Изложбе`
    - project links
    - admin panel link for admins
  - updated:
    - `templates/base.html`
    - `static/css/main.css`

## Tomorrow Handoff

- Saved at: `2026-03-18 15:09:30 CET`
- Status:
  - modularization pass completed
  - root cleanup and archive pass completed
  - admin panel cleanup completed
  - dashboard first-load bug fixed
  - first phishing-hardening pass for the mail client completed
  - remaining active runtime SQLite usage migrated to PostgreSQL
  - bird ringing module cleaned to PostgreSQL-only
  - PostgreSQL verification scripts converted into discovered automated tests
  - full regression suite green after PostgreSQL cleanup
  - navbar ornament removed and adaptive laptop-width top-nav compaction implemented
- New module added today:
  - `collection_bootstrap_support.py`
  - `vehicle_data_support.py`
  - `museum_staff_support.py`
  - `module_access_support.py`
  - `fallback_auth_support.py`
  - `app_ui_support.py`
  - `app_core_support.py`
- Latest known-good validation:
  - `python3 -m py_compile app.py collection_bootstrap_support.py vehicle_data_support.py museum_staff_support.py module_access_support.py fallback_auth_support.py app_ui_support.py app_core_support.py`
  - `python3 -m unittest test_startup_lazy_loading.py test_timesheet_route_refactor.py`
  - `python3 -m unittest test_production_readiness.py test_authorization_regressions.py`
  - later UI/dashboard regressions:
    - `python3 -m unittest test_startup_lazy_loading.py` -> `Ran 21 tests`, `OK`
    - `python3 -m unittest test_timesheet_route_refactor.py` -> `Ran 230 tests`, `OK`
  - PostgreSQL-focused validation:
    - `python3 -m unittest test_postgres_connection.py test_postgres_implementation.py` -> `Ran 8 tests`, `OK`
  - final full-suite validation:
    - `python3 -m unittest discover -s /home/aleksandarlukovic/MuseumInfoSystem -p 'test_*.py'` -> `Ran 295 tests`, `OK`
- Recommended first step next session:
  - start from this progress file
  - keep PostgreSQL as the only live runtime database target
  - if continuing quality work, prioritize either:
    - the remaining mail-client hardening follow-up
    - the Werkzeug bytes-cookie deprecation cleanup
    - or another focused authenticated dashboard/admin polish pass
