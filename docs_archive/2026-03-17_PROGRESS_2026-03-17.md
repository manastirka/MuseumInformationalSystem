# Progress - 2026-03-17

## Scope

Continued the `app.py` modularization pass by extracting the remaining locality and science-news API block that sat between the mineral science routes and the mail routes.

## Work Completed Today

- Added `depot_science_views.py` for depot locality and science-news API implementations.
- Extracted:
  - `/api/depot/localities`
  - `/api/science-news` (`GET`)
  - `/api/science-news` (`POST`)
  - `/api/science-news/<news_id>` (`DELETE`)
  - `/api/depot/locality/<path:locality_name>`
- Replaced the corresponding `app.py` bodies with thin wrappers to preserve routes, decorators, and endpoint names.
- Extended `test_timesheet_route_refactor.py` with delegation coverage for the new wrapper family.
- Added `collection_management_views.py` for collection, mineral, and inventory admin views.
- Extracted collection/admin routes for:
  - cultural heritage
  - standard curator collections
  - meteorites
  - mineral collection and detail pages
  - RRUFF detail redirect/detail
  - add/edit/delete mineral flows
  - inventory book and reconciliation
  - conservation biology
  - collection PDF placeholder redirect
- Replaced the corresponding `app.py` route bodies with thin wrappers and shared helper-based implementations in the new module.
- Extended `test_timesheet_route_refactor.py` with delegation coverage for the extracted collection/mineral/inventory wrapper family.
- Completed the remaining NHM portal modularization for data-search helpers by moving:
  - `/api/nhm/data/search/local`
  - `/api/nhm/data/search/nhm`
  - Serbian/English mineral name translation helpers
  into `nhm_portal_views.py`.
- Replaced the corresponding `app.py` NHM data-search bodies with thin wrappers and added delegation coverage for them.
- Added `travel_finance_views.py` for request pages, travel workflows, route calculations, procurement APIs, and financial-plan APIs.
- Extracted:
  - request/finance pages (`/zahtevi/*`, `/finansije/*`, `/terenska-aktivnost`, `/zahtev-sluzbeni-put`)
  - `/api/field-trip/create`
  - `/api/accommodation/search`
  - `/api/route/calculate`
  - procurement APIs (`/api/nabavka/*`)
  - financial-plan APIs (`/api/finansijski-plan/*`)
- Moved the static route/toll dataset and the repeated Word-export implementations for procurement/financial plan into the new module.
- Replaced the corresponding `app.py` travel/finance bodies with thin wrappers and added delegation coverage for the full extracted family.
- Added `museum_overview_views.py` for the `/admin/museum_databases` overview composition logic.
- Replaced `museum_databases()` in `app.py` with a thin wrapper that keeps lazy `LIBRARY_DATABASE` initialization local and delegates the heavy statistics/context assembly to the new module.
- Backfilled wrapper regression coverage for `/admin/museum_databases` in `test_timesheet_route_refactor.py`.
- Continued the modularization pass by extracting the adjacent admin form/database handlers into shared modules:
  - `museum_content_views.py` now owns add-book, add-visitor, add-research, visitors database, visitor PDF placeholder redirect, and research database handlers.
  - `collection_management_views.py` now owns add-heritage-item and shared add-collection-item form handlers.
- Replaced the corresponding `app.py` bodies with thin wrappers and added delegation coverage for the new wrapper family.
- Added `project_views.py` for the `Projekti` route family wrappers and the inline documentation reader.
- Replaced the `app.py` route bodies for:
  - `/admin/projekti`
  - `/admin/projekti/dokumentacija`
  - `/admin/projekti/space-planner`
  - `/api/projekti/space-planner` (`GET`/`POST`)
  - `/projekti_files/<path:filename>`
  with thin delegators into the new module.
- Moved the project documentation extraction/metadata logic out of `app.py` into `project_views.py`, while leaving the large space-planner constants and helper/state functions in place for the next slice.
- Extended `test_timesheet_route_refactor.py` with delegation coverage for the extracted `Projekti` wrapper family.
- Continued the `Projekti` extraction by moving the space-planner state/normalization helpers into `project_views.py`:
  - auto-detected space normalization
  - planner default-state construction
  - planner load/save with backup rotation
  - space payload sanitization
- `app.py` now retains only the seeded planner datasets/constants and passes those into `project_views.py` for the `GET`/`POST` space-planner API wrappers.
- Moved the remaining planner metadata/config scaffolding into `project_views.py` as well:
  - plan filenames and image sizes
  - depot area annotations
  - auto-layout version
  - planner view definitions
  - room-library definitions
  - shared planner term suggestions
- `app.py` now aliases those constants from `project_views.py`, leaving only the two large seeded space payload lists inline.

## Validation

- `python3 -m py_compile app.py depot_science_views.py test_timesheet_route_refactor.py`
- `python3 -m unittest test_timesheet_route_refactor.py`
- `python3 -m unittest test_production_readiness.py test_authorization_regressions.py test_startup_lazy_loading.py test_timesheet_route_refactor.py`
- `python3 -m py_compile app.py collection_management_views.py test_timesheet_route_refactor.py`
- `python3 -m unittest test_timesheet_route_refactor.py`
- `python3 -m unittest test_production_readiness.py test_authorization_regressions.py test_startup_lazy_loading.py test_timesheet_route_refactor.py`
- `python3 -m py_compile app.py nhm_portal_views.py test_timesheet_route_refactor.py`
- `python3 -m unittest test_timesheet_route_refactor.py`
- `python3 -m unittest test_production_readiness.py test_authorization_regressions.py test_startup_lazy_loading.py test_timesheet_route_refactor.py`
- `python3 -m py_compile app.py travel_finance_views.py test_timesheet_route_refactor.py`
- `python3 -m unittest test_timesheet_route_refactor.py`
- `python3 -m unittest test_production_readiness.py test_authorization_regressions.py test_startup_lazy_loading.py test_timesheet_route_refactor.py`
- `python3 -m py_compile app.py collection_management_views.py museum_content_views.py museum_overview_views.py test_timesheet_route_refactor.py`
- `python3 -m unittest test_timesheet_route_refactor.py`
- `python3 -m unittest test_production_readiness.py test_authorization_regressions.py test_startup_lazy_loading.py test_timesheet_route_refactor.py`
- `python3 -m py_compile app.py project_views.py test_timesheet_route_refactor.py`
- `python3 -m unittest test_timesheet_route_refactor.py`
- `python3 -m unittest test_production_readiness.py test_authorization_regressions.py test_startup_lazy_loading.py test_timesheet_route_refactor.py`
- Final test count after this slice: `Ran 254 tests`
- Result: `OK`

## Current State

- The route block immediately after the mineral science APIs is now extracted.
- `app.py` keeps only wrapper functions for these endpoints.
- Wrapper regression coverage now includes depot locality and science-news routes.
- The collections/minerals/inventory admin block has been moved out of the monolith into a shared module.
- `app.py` is materially smaller in the curator collection and mineral management area.
- Wrapper regression coverage now includes the extracted collection/mineral/inventory routes.
- NHM portal-related route logic is now consolidated in `nhm_portal_views.py`, including the specimen/data search helpers that were previously left behind in `app.py`.
- The travel/finance workflow block has been moved out of the monolith into `travel_finance_views.py`.
- `app.py` is materially smaller in the request/procurement/financial-plan area as well.
- Wrapper regression coverage now includes the extracted travel and finance endpoints.
- The museum databases overview now renders through `museum_overview_views.py` instead of building its entire context inline inside `app.py`.
- The add-book, add-heritage-item, add-collection-item, add-visitor, add-research, visitors database, and research database route handlers are now extracted behind shared modules as well.
- The `Projekti` route family now delegates through `project_views.py`, and the inline project-document reader logic is no longer embedded in `app.py`.
- The `Projekti` planner state/load/save/sanitize logic is also out of `app.py`; the remaining inline portion is now mostly seeded planner configuration/data.
- The last two seeded `Projekti` planner payloads, `PROJECT_DEPOT_AUTO_DETECTED_SPACES` and `PROJECT_AUTO_DETECTED_SPACES`, are now owned by `project_views.py` too.
- The `Projekti` section in `app.py` is now down to route wrappers plus constant aliases, which effectively completes that extraction pass.
- The remaining map support logic is now split out of `app.py` into `maps_terrain_support.py` and `maps_profile_support.py`.
- `app.py` keeps stable alias names for the terrain/profile helpers so the existing route wrappers and tests did not need behavioral changes.
- Duplicate biodiversity/map cache state that already lived inside the dedicated view modules has been removed from `app.py`.
- The public/session/dashboard/admin-home route logic is now extracted into `core_app_views.py`.
- `app.py` keeps the decorated route wrappers for `/`, `/login`, `/logout`, `/change_password`, `/dashboard`, `/dashboard-classic`, `/mineral_database`, `/admin`, and `/set_language`, but the request logic is no longer inline there.
- Wrapper regression coverage now includes the new core-session/core-dashboard wrapper family.
- The dashboard widget customization route now delegates through `admin_user_management_views.py` instead of updating preferences inline inside `app.py`.
- Wrapper regression coverage now includes `/dashboard/customize` as part of the extracted admin/user-management surface.
- The weather/news bootstrap helpers are now extracted into `dashboard_data_support.py`.
- `app.py` keeps stable aliases for the dashboard weather/news helpers and warning URL so the extracted dashboard integration routes did not need signature changes.
- The exhibits/exhibitions/news bootstrap data and statistics helpers are now extracted into `museum_content_support.py`.
- `app.py` keeps compatibility wrappers for `load_exhibitions_data()`, `load_news_data()`, `build_exhibitions_database()`, `build_news_database()`, `get_exhibit_statistics()`, and `get_exhibition_statistics()`, while the large seeded `EXHIBITS_DATABASE` payload now lives outside the monolith.
- The QR/image-upload helper/config surface is now extracted into `collection_access_support.py`.
- `app.py` now initializes one shared collection-access support object and re-exports the existing helper names for QR collection lookup, label metadata, batch image upload config, and access checks.

## Latest Validation

- `python3 -m py_compile app.py collection_access_support.py museum_content_support.py dashboard_data_support.py test_timesheet_route_refactor.py test_startup_lazy_loading.py`
- `python3 -m unittest test_timesheet_route_refactor.py`
- `python3 -m unittest test_production_readiness.py test_authorization_regressions.py test_startup_lazy_loading.py test_timesheet_route_refactor.py`
- Final test count after this slice: `Ran 264 tests`
- Result: `OK`

## Next Target

Continue with the remaining large inline collection/bootstrap surface that still lives directly in `app.py`, especially the collection loader/cache helpers and seeded database payloads that still sit above the extracted wrapper sections.

## Tomorrow Handoff

- Saved at: `2026-03-17 14:37:33 CET`
- Status: modularization pass completed cleanly for today; current extracted support/modules compile and the regression suites are green.
- Most recent extractions completed today:
  - `dashboard_data_support.py`
  - `museum_content_support.py`
  - `collection_access_support.py`
- `app.py` now mainly keeps wrappers, aliases, and bootstrap glue for those areas instead of inline implementations.
- Latest known-good validation:
  - `python3 -m py_compile app.py collection_access_support.py museum_content_support.py dashboard_data_support.py test_timesheet_route_refactor.py test_startup_lazy_loading.py`
  - `python3 -m unittest test_timesheet_route_refactor.py`
  - `python3 -m unittest test_production_readiness.py test_authorization_regressions.py test_startup_lazy_loading.py test_timesheet_route_refactor.py`
  - Result: `Ran 264 tests`, `OK`
- Recommended first step tomorrow:
  - inspect the remaining inline collection/bootstrap helpers in `app.py`
  - extract the next coherent block around collection loader/cache helpers and seeded payloads
  - keep `app.py` names stable as wrappers or aliases so existing route tests continue to pass
