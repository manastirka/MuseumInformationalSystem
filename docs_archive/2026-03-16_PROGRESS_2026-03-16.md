# Progress - 2026-03-16

## Scope

Continued breaking down the `app.py` monolith into route-focused modules with thin wrappers kept in place to preserve endpoint names and behavior.

## Work Completed Today

- Extracted scientific papers and paper enrichment routes into `scientific_paper_views.py`.
- Extracted bird ringing routes into `bird_ringing_views.py`.
- Extracted exhibition planner and terminology routes into `exhibition_planner_views.py`.
- Extracted mineral science APIs into `mineral_science_views.py`.
- Moved the chemical formula formatter into `mineral_science_views.py`.
- Kept `app.py` wrappers stable so existing URLs and endpoint names continue to work.
- Extended `test_timesheet_route_refactor.py` with delegation coverage for all newly extracted route families.

## Validation

- `python3 -m py_compile app.py scientific_paper_views.py bird_ringing_views.py exhibition_planner_views.py mineral_science_views.py test_timesheet_route_refactor.py`
- `python3 -m unittest test_production_readiness.py test_authorization_regressions.py test_startup_lazy_loading.py test_timesheet_route_refactor.py`
- Final test count after this slice: `Ran 201 tests`
- Result: `OK`

## Current State

- `app.py` is materially smaller and a large portion of the high-traffic route families are now outside the monolith.
- Route compatibility has been preserved through thin wrapper functions in `app.py`.
- Startup and refactor regression coverage remains green.

## Next Target

Continue with the remaining block between the mineral APIs and the mail routes:

- `/api/depot/localities`
- `/api/science-news`
- `/api/science-news/<news_id>`
- `/api/depot/locality/<path:locality_name>`

After that, continue reducing remaining helper/global coupling in `app.py`.
