[Unit]
Description=Run the MuseumInfoSystem backup nightly

[Timer]
# Every day at 02:30. Persistent=true runs a missed backup after downtime.
OnCalendar=*-*-* 02:30:00
Persistent=true
RandomizedDelaySec=300

[Install]
WantedBy=timers.target
