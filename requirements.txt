# Core Flask
Flask>=2.3.0
Flask-SQLAlchemy>=3.0.0
Flask-Babel>=4.0.0
Werkzeug>=2.3.0
python-dotenv>=1.0.0

# Security
Flask-WTF>=1.2.0
Flask-Limiter>=3.5.0
Flask-Session>=0.5.0
Flask-Talisman>=1.1.0
cryptography>=41.0.0
bcrypt>=4.1.0

# Database
pymysql>=1.0.0
psycopg[binary]>=3.1.0

# Image Processing
Pillow>=10.0.0
qrcode[pil]>=7.4.2
# [binary] bundles libvips (no system package needed) - used only by the
# fototeka derivative worker, never imported in the web process
pyvips[binary]>=2.2.0

# Scientific / GIS
numpy>=1.24.0
rasterio>=1.3.0
scipy>=1.11.0
psutil>=5.9.0

# PDF Generation
reportlab>=4.0.0

# QR Code Generation (used by qr_label_views)
qrcode[pil]>=7.4.0

# HTTP Client
requests>=2.31.0
boto3>=1.34.0

# HTML Parsing
beautifulsoup4>=4.12.0
lxml>=5.0.0

# Word Document Generation
python-docx>=1.1.0

# Excel (used by the K-R досије import CLI: uvezi-kr-dosije)
openpyxl>=3.1.0
# .xls chemistry files (RRUFF local data via mineral_science_views);
# xlrd 2.x dropped everything but clean .xls — stay on 1.x
xlrd>=1.2.0,<2

# Production Server
gunicorn>=21.0.0

# Caching (optional but recommended)
redis>=5.0.0

# Email
Flask-Mail>=0.9.1

# JSON Logging
python-json-logger>=2.0.7

# Password Validation
password-strength>=0.0.3.post2

# Error Tracking (optional)
sentry-sdk[flask]>=1.40.0
opentelemetry-api>=1.28.0
opentelemetry-sdk>=1.28.0
opentelemetry-exporter-otlp>=1.28.0
opentelemetry-instrumentation-flask>=0.49b0
opentelemetry-instrumentation-requests>=0.49b0

# Testing (development)
pytest>=7.4.0
pytest-flask>=1.3.0
pytest-cov>=4.1.0

# Code Quality (development)
black>=23.0.0
flake8>=6.0.0
mypy>=1.5.0
